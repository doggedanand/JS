// ================================================================
// It will select all siblings of selected element

$(document).ready(function () {
  $("span").siblings().css("background-color", "red");
});
