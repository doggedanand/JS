// ================================================================
// It will return the element based on indexing of selected element

$(document).ready(function () {
  $("div").eq(1).css("background-color", "red");
  $("div").eq(1).css("color", "yellow");
  $("div").eq(1).css("font-weight", "bold");
});
