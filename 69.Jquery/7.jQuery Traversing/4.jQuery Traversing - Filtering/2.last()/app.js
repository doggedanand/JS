// ================================================================
// It will return the last element of selected element

$(document).ready(function () {
  $("div").last().css("background-color", "red");
  $("div").last().css("color", "yellow");
  $("div").last().css("font-weight", "bold");
});
