// ================================================================
// It will remove the selected  element on user's click

$(document).ready(function () {
  $("button").click(function () {
    $("div").remove();
  });
});
