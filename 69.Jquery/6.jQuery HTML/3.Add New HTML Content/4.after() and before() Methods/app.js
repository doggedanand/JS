// ================================================================
// It will add the data before and after according to method

$(document).ready(function () {
  // will add the data before image
  $("img").before("before");
  // will add the data after image
  $("img").after("after");
});
